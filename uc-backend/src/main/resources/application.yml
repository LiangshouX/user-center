spring:
  application:
    name: uc-backend

  # DataSource Config
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: root
    # url: jdbc:mysql://localhost:3306/user_center
server:
  port: 8080

